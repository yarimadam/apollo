global:
  checkNewVersion: false
  sendAnonymousUsage: false

log:
  level: INFO

accessLog: {}

# No forwardedHeaders.trustedIPs: Traefik is the edge, so client-supplied
# X-Forwarded-* headers are always overwritten, never trusted.
# aliasHeadersStrategy drops e.g. Remote_User, which some backends read as
# Remote-User, so clients can't spoof the headers Authelia sets.
entryPoints:
  web:
    address: ":80"
    http:
      aliasHeadersStrategy: delete
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
    http:
      aliasHeadersStrategy: delete
    http3: {}
  admin:
    address: ":8443"
    http:
      aliasHeadersStrategy: delete

api:
  dashboard: true

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

certificatesResolvers:
  letsencrypt:
    acme:
      storage: /data/acme.json
      httpChallenge:
        entryPoint: web
