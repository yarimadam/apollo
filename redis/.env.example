# Password for the Redis instance. Copy the same value into REDIS_PASSWORD
# in aiostreams/.env and aiometadata/.env, and into
# authelia/.env AUTHELIA_SESSION_REDIS_PASSWORD.
# Generate via: openssl rand -hex 24
PASSWORD=
